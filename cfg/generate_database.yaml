# Core parameters ####################################################
root_dir:     /home/jmahler/jacky_working/GPIS
cache_dir:    /home/jmahler/jacky_working/GPIS/data/gcs

# DB generation params
gen_datasets:
  - Cat50_ModelDatabase
  - dexnet_physical_experiments
  - KIT
gen_dataset_start: 0

shape_data_dir: /mnt/terastation/shape_data
database_dir: &id_database_dir /mnt/wd_ssd/dexnet/db/
database_cache_dir: *id_database_dir
database_name: patches_db.hdf5
results_database_name: results.hdf5

# Object gen params
sdf_dim: 100
sdf_padding: 5
obj_scale: 0.09
gripper_size: 0.1
mass_thresh: 0.01
default_mass: 1.0
obj_density: 4000
stp_min_prob: 0.01
#preproc_script: scripts/decimation.mlx

# Convex decomposition
cvx_decomp:
  hacd_cmd_template: 'hacd %s %d %d %d %d %d %d %d'
  min_num_clusters: 2
  max_concavity: 1
  invert_input_faces: 1
  extra_dist_points: 1
  add_faces_points: 1
  connected_components_dist: 10
  target_num_triangles: 1000

dataset_counts: data/counts.txt
sleep_time: 10

